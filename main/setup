#!/usr/bin/qsh
export -s TGTLIB=OSSILE
system "crtlib $TGTLIB"
export WORKDIR=$(pwd)/$(dirname $0)
cd $WORKDIR/udtf_machine_attrs && setup
cd $WORKDIR/udtf_image_catalog_details && setup
cd $WORKDIR/udtf_cartridge_info && setup